# Base de datos
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/taller_pc

# Sesión (generar con: openssl rand -base64 32)
SESSION_SECRET=cambiar-por-un-secreto-seguro-en-produccion

# Usuario administrador inicial (se crea en el seed si no existe)
ADMIN_EMAIL=admin@taller.com
ADMIN_PASSWORD=admin123
ADMIN_NOMBRE=Administrador

# Configuración del taller (se crea en el seed si no existe)
TALLER_NOMBRE=Mi Taller
TALLER_MONEDA=MXN
